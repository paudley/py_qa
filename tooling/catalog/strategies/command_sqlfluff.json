{
  "schemaVersion": "1.0.0",
  "id": "command_sqlfluff",
  "type": "command",
  "description": "Construct sqlfluff lint/fix commands with catalog-provided defaults.",
  "implementation": "pyqa.tooling.strategies.sqlfluff_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke sqlfluff."
    },
    "isFix": {
      "type": "boolean",
      "description": "Indicates whether the command should run in fix mode."
    }
  }
}

{
  "schemaVersion": "1.0.0",
  "id": "command_yamllint",
  "type": "command",
  "description": "Construct yamllint commands with catalog-provided defaults.",
  "implementation": "pyqa.tooling.strategies.yamllint_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke yamllint."
    }
  }
}

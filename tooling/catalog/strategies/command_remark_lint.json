{
  "schemaVersion": "1.0.0",
  "id": "command_remark_lint",
  "type": "command",
  "description": "Construct remark-lint commands with catalog-provided defaults.",
  "implementation": "pyqa.tooling.strategies.remark_lint_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke remark-lint."
    },
    "isFix": {
      "type": "boolean",
      "description": "Indicates whether the command should run in fix mode."
    }
  }
}

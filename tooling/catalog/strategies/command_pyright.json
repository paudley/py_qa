{
  "schemaVersion": "1.0.0",
  "id": "command_pyright",
  "type": "command",
  "description": "Construct pyright commands with catalog-provided defaults.",
  "implementation": "pyqa.tooling.strategies.pyright_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke pyright."
    }
  }
}

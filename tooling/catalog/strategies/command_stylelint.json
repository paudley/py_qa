{
  "schemaVersion": "1.0.0",
  "id": "command_stylelint",
  "type": "command",
  "description": "Construct stylelint commands with configuration, formatter defaults, and quiet flags.",
  "implementation": "pyqa.tooling.strategies.command_stylelint",
  "config": {
    "base": {
      "type": "array",
      "description": "Base command arguments; defaults to ['stylelint']."
    },
    "isFix": {
      "type": "boolean",
      "description": "Indicate whether the command is used for a fix action."
    }
  }
}

{
  "schemaVersion": "1.0.0",
  "id": "command_mypy",
  "type": "command",
  "description": "Construct mypy commands with catalog-provided defaults.",
  "implementation": "pyqa.tooling.strategies.mypy_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke mypy."
    }
  }
}

{
  "schemaVersion": "1.0.0",
  "id": "command_kube_linter",
  "type": "command",
  "description": "Construct kube-linter commands with catalog-provided defaults.",
  "implementation": "pyqa.tooling.strategies.kube_linter_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke kube-linter."
    }
  }
}

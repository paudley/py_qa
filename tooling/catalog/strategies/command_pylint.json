{
  "schemaVersion": "1.0.0",
  "id": "command_pylint",
  "type": "command",
  "description": "Construct pylint commands with catalog-provided defaults.",
  "implementation": "pyqa.tooling.strategies.pylint_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke pylint."
    }
  }
}

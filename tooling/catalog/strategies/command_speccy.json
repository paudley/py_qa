{
  "schemaVersion": "1.0.0",
  "id": "command_speccy",
  "type": "command",
  "description": "Construct Speccy commands with enforced JSON reporting.",
  "implementation": "pyqa.tooling.strategies.speccy_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke Speccy."
    }
  }
}

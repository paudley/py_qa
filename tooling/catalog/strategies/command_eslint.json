{
  "schemaVersion": "1.0.0",
  "id": "command_eslint",
  "type": "command",
  "description": "Construct ESLint commands with catalog-provided defaults.",
  "implementation": "pyqa.tooling.strategies.eslint_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke ESLint."
    },
    "isFix": {
      "type": "boolean",
      "description": "Indicates whether the command runs in --fix mode."
    }
  }
}

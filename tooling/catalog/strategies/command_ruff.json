{
  "schemaVersion": "1.0.0",
  "id": "command_ruff",
  "type": "command",
  "description": "Construct ruff commands for linting or fixing.",
  "implementation": "pyqa.tooling.strategies.ruff_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke ruff."
    },
    "mode": {
      "type": "string",
      "required": true,
      "description": "Mode to run in (lint or fix)."
    }
  }
}

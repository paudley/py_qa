{
  "schemaVersion": "1.0.0",
  "id": "command_isort",
  "type": "command",
  "description": "Construct isort commands with catalog-provided defaults.",
  "implementation": "pyqa.tooling.strategies.isort_command",
  "config": {
    "base": {
      "type": "array",
      "required": true,
      "description": "Base command used to invoke isort."
    }
  }
}

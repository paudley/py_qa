{
  "schemaVersion": "1.0.0",
  "id": "command_pyupgrade",
  "type": "command",
  "description": "Generate pyupgrade commands with automatic Python version flags.",
  "implementation": "pyqa.tooling.strategies.pyupgrade_command",
  "config": {
    "base": {
      "type": "array",
      "description": "Base command arguments (defaults to ['pyupgrade'])."
    }
  }
}

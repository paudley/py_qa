{
  "schemaVersion": "1.0.0",
  "name": "gofmt",
  "description": "Go formatter.",
  "languages": ["go"],
  "phase": "lint",
  "runtime": {
    "type": "binary",
    "versionCommand": ["go", "version"]
  },
  "actions": [
    {
      "name": "format",
      "command": {
        "strategy": "command_gofmt",
        "config": {
          "base": ["gofmt", "-w"]
        }
      },
      "isFix": true,
      "description": "Format Go source files with gofmt."
    },
    {
      "name": "check",
      "command": {
        "strategy": "command_gofmt",
        "config": {
          "base": ["gofmt", "-l"]
        }
      },
      "description": "List Go files requiring gofmt.",
      "parser": {
        "strategy": "parser_text",
        "config": {
          "transform": "pyqa.tools.builtin_helpers._parse_gofmt_check"
        }
      }
    }
  ],
  "fileExtensions": [".go"],
  "documentation": {
    "help": {
      "path": "docs/gofmt_help.txt",
      "format": "text"
    },
    "commandHelp": {
      "path": "docs/gofmt_cmd_help.txt",
      "format": "text"
    }
  }
}
